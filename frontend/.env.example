# Set this to point at a deployed backend (e.g. the API Gateway URL printed by `npm run deploy`).
# Leave UNSET / blank in local dev — Vite proxies /api and /health to http://localhost:3000.
# VITE_API_URL=https://abc123.execute-api.us-east-1.amazonaws.com
